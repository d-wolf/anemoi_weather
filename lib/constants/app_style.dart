import 'package:flutter/material.dart';

class AppStyle {
  const AppStyle._();

  static const spaceXSmall = 4.0;
  static const spaceSmall = 8.0;
  static const spaceMedium = 16.0;
  static const spaceLarge = 20.0;
  static const spaceXLarge = 24.0;

  static const insetsZero = EdgeInsets.zero;
  static const insetsXSmall = EdgeInsets.all(spaceXSmall);
  static const insetsSmall = EdgeInsets.all(spaceSmall);
  static const insetsMedium = EdgeInsets.all(spaceMedium);
  static const insetsLarge = EdgeInsets.all(spaceLarge);
  static const insetsXLarge = EdgeInsets.all(spaceXLarge);
}
